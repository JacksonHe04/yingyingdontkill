## 前端

* 技术栈
  * Next.js
  * App Router
  * Tailwind CSS
  * CSS Modules
  * Three.js + React Three Fiber
  * Frame Motion
* JSON 数据渲染

## 后端

* 收集用户数据
  * 通过 AI 提问的输入和输出
  * 留言
  * 地理位置
  * 邮箱
  * 用户名
* 数据库
  * 关系型数据表
  * 文档表
* AI 助手（小缨缨）
  * 系统提示词
  * 常规问答
    * 工作流
      * 意图识别
      * 检索信息
      * 拼接提示词
    * 规划可视化
  * 预置问题
    * 给定输入
    * 拼接提示词
* 标签墙点亮记录
  * 点亮的标签
  * 点亮时间
  * 用户标识

## 关于图片

例如：网站logo、导航栏头像、公司logo、各种图片等
<!-- TODO -->
目前还没有明确的方案，目前是可以把所有图片存储在一个云存储图床服务中，可以通过链接来访问图片。
为了方便，我们会给图床中的每一个图片一个唯一的文件名，我们需要在组件渲染的时候，直接根据 JSON 中的某个类似于 name 这种字段一致的文件名来访问图片，而无需新增专门存储文件名与链接的字段。